name: Publish to JSR

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # required for JSR OIDC auth (no token needed)
    steps:
      - uses: actions/checkout@v4
      - run: npx jsr publish
